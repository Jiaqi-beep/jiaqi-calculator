import styles from "../styles/Calculator.module.css";

export default function Calculator() {
    return (
        <div className={styles.border}>
            <di className={styles.buttonWrapper}></di>
        </div>
    )
}